---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import SkillCard from '../components/SkillCard.astro';
import { Image } from 'astro:assets';

// Asegúrate de que la ruta sea correcta
import profileImg from '../assets/images/Yo.jpg'; 
import fiImg from '../assets/images/fi.jpg';
import enp9Img from '../assets/images/enp9.jpg';
import cpcfiImg from '../assets/images/cpcfi.jpg';
import acatlanImg from '../assets/images/acatla.jpg';
import icpcImg from '../assets/images/icpc.jpg';
import ieeextremeImg from '../assets/images/ieeextreme.jpg';
import escomImg from '../assets/images/escom.jpg';
import platicaImg from '../assets/images/platica.jpg';

// --- DATA ---

const socialLinks = [
  { 
    name: "Github",
    icon: "devicon-github-original", 
    link: "https://github.com/zums-stuff" 
  },
  { 
    name: "LinkedIn",
    icon: "devicon-linkedin-plain", 
    link: "https://www.linkedin.com/in/zum-hernandez/" 
  },
  { 
    name: "Email",
    image: "https://cdn.simpleicons.org/gmail/white",
    link: "mailto:jafet.hernandez.ed@gmail.com" 
  },
];

const cpProfiles = [
  { 
    name: "Codeforces",
    image: "https://cdn.simpleicons.org/codeforces/white", 
    link: "https://codeforces.com/profile/zum" 
  },
  { 
    name: "CSES", 
    image: "https://cses.fi/logo.png?1", 
    link: "https://cses.fi/user/314973",
    class: "cses-logo"
  },
  { 
    name: "LeetCode", 
    image: "https://cdn.simpleicons.org/leetcode/white", 
    link: "https://leetcode.com/u/zum04/" 
  },
];

const coreSkills = [
  { name: "C / C++", level: 85 },
  { name: "Algorithms & DS", level: 80 },
  { name: "Python", level: 40 },
  { name: "C#", level: 65 },
  { name: "Java", level: 75 },
  { name: "HTML & CSS", level: 75 },
];

const skills = [
  { name: "C / C++", icon: "devicon-cplusplus-plain colored" },
  { name: "C#", icon: "devicon-csharp-plain colored" },
  { name: "Python", icon: "devicon-python-plain colored" },
  { name: "Java", icon: "devicon-java-plain colored" },
  { name: "OpenGL", icon: "devicon-opengl-plain colored" },
  { name: "Unity", icon: "devicon-unity-plain " },
  { name: "Godot", icon: "devicon-godot-plain colored" },
  { name: "Linux", icon: "devicon-linux-plain" },
  { name: "Arch Linux", icon: "devicon-archlinux-plain colored" },
  { name: "Git", icon: "devicon-git-plain colored" },
  { name: "GitHub", icon: "devicon-github-original" },
  { name: "Flutter", icon: "devicon-flutter-plain colored" },
  { name: "LᴬTᴱX", icon: "devicon-latex-original" },
  { name: "HTML5", icon: "devicon-html5-plain colored" },
  { name: "CSS3", icon: "devicon-css3-plain colored" },
  { name: "Astro", icon: "devicon-astro-plain colored" },
  { name: "JavaScript", icon: "devicon-javascript-plain colored" },
  { name: "Visual Studio 2022", icon: "devicon-visualstudio-plain colored" },
  { name: "Visual Studio Code", icon: "devicon-vscode-plain colored" },
  { name: "Obsidian", image: "https://cdn.simpleicons.org/obsidian/A882FF" },
  { name: "Illustrator", icon: "devicon-illustrator-plain" },
];

const experience = [
  {
    role: "Competitor",
    company: "IEEEXtreme 19.0",
    date: "Oct 2025",
    description: "Collaborated in a team to solve 12 algorithmic problems in a 24-hour coding competition, enhancing teamwork and problem-solving skills.",
    image: ieeextremeImg,
  },
  {
    role: "Participant & Backend Dev",
    company: "NASA International Space Apps Challenge",
    date: "Oct 2025",
    description: "Developed a Python-based backend using Flask for 'Meteor Atlas'. Implemented the NASA NEO API to serve processed data.",
  },
  {
    role: "Competitor (14th Place)",
    company: "XVII Donald Knuth contest",
    date: "Aug 2025",
    description: "Competitor in the seventeenth edition of the Donald Knuth Contest at FES Acatlán, where I achieved 14th place out of 40, solving 2 problems during the contest.",
    image: acatlanImg,
  },
  {
    role: "Academic Coach",
    company: "CPCFI - UNAM School of Engineering",
    date: "Jun 2025 - Present",
    description: "Mentoring aspiring competitive programmers in algorithms and data structures.",
    image: cpcfiImg,
  },
  {
    role: "Competitor (129th Place)",
    company: "ICPC Gran Premio de México 2025",
    date: "Mar 2025",
    description: "Solved advanced algorithmic challenges under pressure, developing resilience and collaboration.",
    image: icpcImg,
  },
  {
    role: "Competitor (7th Place)",
    company: "1st Abraham Macías Contest",
    date: "Feb 2025",
    description: "Solved 3 competitive problems in a high-pressure environment at ESCOM IPN.",
    image: escomImg,
  }
];

const education = [   
  {
    role: "Bachelor of Computer Engineering",
    company: "UNAM School of Engineering",
    date: "Aug 2024 - Expected 2029",
    description: "Focused on Software Engineering, Algorithms, and System Architecture. Active member of the Competitive Programming Club.",
    image: fiImg,
  },
  {
    role: "High School Diploma",
    company: "National Preparatory School No. 9 Pedro de Alba (UNAM)",
    date: "Aug 2021 - Jun 2024",
    description: "Specialized in Physics and Mathematics (Area 1). Graduated with a strong foundation in calculus and scientific reasoning.",
    image: enp9Img,
  },
];

const projects = [
  {
    title: "Meteor Atlas (NASA Space Apps)",
    status: "FINISHED",
    color: "#fbbf24", 
    description: "Simulation and analysis of asteroid impacts using Python, Flask, and the NASA API.\n\n",
    tags: ["Python", "Flask", "NASA API"],
    link: "https://github.com/zums-stuff/Meteor-Madness"
  },
  {
    title: "CPCFI Learning Platform",
    status: "DEPLOYED AND BUILDING",
    color: "#10b981", 
    description: "Static website deployed on GitHub Pages to provide curated learning materials and editorials for competitive programming enthusiasts.",
    tags: ["HTML", "CSS", "GitHub Pages"],
    link: "https://zums-stuff.github.io/CPCFI-Webpage/"
  },
  {
    title: "2D Animation Engine",
    status: "FINISHED",
    color: "#3b82f6", 
    description: "Sequential animation system implemented in C++ and OpenGL. Applies linear algebra concepts to computer graphics.",
    tags: ["C++", "OpenGL", "Linear Algebra"],
    link: "https://github.com/zums-stuff"
  },
  {
    title: "Competitive Programming Solutions",
    status: "ACTIVE REPO",
    color: "#8b5cf6", 
    description: "Automated massive repository with optimized solutions for Codeforces, LeetCode, and CSES. Personal reference for algorithms and data structures.",
    tags: ["C++", "Algorithms", "Data Structures"],
    link: "https://github.com/zums-stuff/Competitive-Programming"
  },
  {
    title: "D&D-like Game System",
    status: "PROTOTYPE (NOT DEPLOYED)",
    color: "#f43f5e", 
    description: "RPG game architecture built in Unity with basic multiplayer support, designed from scratch using C# and standard packages in Unity.",
    tags: ["Unity", "C#", "Game Dev"],
    link: "https://github.com/zums-stuff"
  }
];

const relevantCoursework = [
  "Data Structures & Algorithms I and II",
  "Object-Oriented Programming",
  "Linear Algebra",
  "Software Engineering",
  "Multivariable Calculus",
  "Diferential Ecuations",
  "Automata Theory",
  "Discrete Mathematics",
];
---

<Layout title="Jafet 'Zum' Hernandez | Portfolio">
  
  <nav class="top-nav">
    <div class="nav-container">
        <button class="nav-item active" data-target="home">Home</button>
        <button class="nav-item" data-target="resume">About</button>
        <button class="nav-item" data-target="portfolio">Works</button>
        <button class="nav-item" data-target="contact">Contact</button>
        
        <div class="nav-separator"></div>

        <a href={import.meta.env.BASE_URL + "resume.pdf"} download="Jafet_Hernandez_Resume.pdf" class="nav-download-btn" style="text-align: center">
            Download Resume<i class="fas fa-download" style="font-size:0.8em;"></i>
        </a>
    </div>
  </nav>

  <main class="container main-content">
    
    <div id="home" class="view-section active">
        <section class="hero">
            <div class="hero-grid">
                
                <div class="image-column">
                    <Image src={profileImg} alt="Jafet Hernandez" class="profile-pic" />
                </div>

                <div class="content-column">
                    <div class="profile-header">
                        <h1>Jafet <span class="text-gradient">"Zum"</span> Hernandez</h1>
                        <span class="tagline" style="margin-bottom:5rem">> [COMPETITIVE PROGRAMMER & WIP COMPUTER ENGINEER]<div class="cursor" style="white-space: nowrap;">_</div></span>
                        
                        <p></p>
                        <div class="social-links">
                            {socialLinks.map(social => (
                                
                                <a href={social.link} target="_blank" class="social-btn" aria-label={social.name}>
                                    {social.image ? (
                                        <img src={social.image} alt={social.name} class="social-img"/>
                                    ) : (
                                        <i class={social.icon}></i>
                                    )}
                                </a>
                            ))}
                        </div>

                        <div class="cp-badges">
                            {cpProfiles.map(cp => (
                                <a href={cp.link} target="_blank" class="cp-link">
                                    <img src={cp.image} alt={cp.name} class={`cp-img ${cp.class || ''}`} />
                                    {cp.name}
                                </a>
                            ))}
                        </div>
                    </div>
                </div>

            </div>
        </section>
        <br><br>
        <hr style="color: rgba(230, 154, 135, 0.628)">
        <h2 class="section-title" style="margin-top: 3rem; border-color: rgba(175, 111, 89, 0); text-align: center"><span class="text-gradient">WHO AM I?</span></h2>
            <div class="bio-text" style="text-align: center">
                <p>
                    Computer Engineering student passionate about C++, algorithms, and Competitive Programming. 
                    Currently studying Computer Engineering (Expected 2029), competing in ICPC, building educational tools for the community, and learning something new everyday.
                </p>
            </div>

        <section class="skills-section" style="margin-top: 4rem;">
            <h2 class="section-title" style="text-align: center; border-color: rgba(0,0,0,0);">TECHNICAL SKILLS & TOOLS</h2>
            <div class="grid-skills">
                {skills.map((skill) => (
                <SkillCard class="skill-card-compact">
                    <div class="skill-content-compact">
                        {skill.image ? (
                            <img src={skill.image} alt={skill.name} class="icon-small" style="width: 1.1rem; height: 1.1rem; object-fit:contain;" />
                        ) : (
                            <i class={`${skill.icon} icon-small`}></i>
                        )}
                        <span class="skill-name-compact">{skill.name}</span>
                    </div>
                </SkillCard>
                ))}
            </div>
        </section>
        
        <br>
        
        <section class="core-skills-section">
            <h2 class="section-title" style="text-align: center; border-color: rgba(0,0,0,0);">CORE EXPERTISE</h2>
            <div class="core-skills-grid">
                {coreSkills.map((skill) => (
                    <div class="core-skill-item">
                        <div class="skill-info">
                            <span class="skill-name">{skill.name}</span>
                            <span class="skill-percentage">{skill.level}%</span>
                        </div>
                        <div class="progress-container-core">
                            <div class="progress-bar-core" style={`width: ${skill.level}%;`}></div>
                        </div>
                    </div>
                ))}
            </div>
        </section>
    </div>

    <div id="resume" class="view-section hidden">
        <section class="exp-edu-section" style="margin-top: -3rem">
            <div class="columns-container">
                <div class="column-block">
                    <h2 class="section-title">Experience</h2>
                    <div class="timeline">
                        {experience.map((item, i) => (
                            <div class="timeline-item">
                                <div class="timeline-marker"></div>
                                <div class="timeline-content">
                                    <span class="timeline-date">{item.date}</span>
                                    <h3 class="role-title">{item.role}</h3>
                                    <span class="company-name">{item.company}</span>
                                    <p class="description-text">{item.description}</p>
                                    {item.image && (
                                        <Image 
                                            src={item.image} 
                                            alt={item.company} 
                                            style={`
                                                margin-top: 0.5rem; 
                                                height: 8rem; 
                                                width: 100%; 
                                                object-fit: cover; 
                                                object-position: ${i === 1 ? '50% 60%': (i === 2) ? '50% 20%' : (i === 5) ? '50% 70%' : 'center'}; 
                                                border-radius: 5px;
                                            `}
                                        />
                                    )}
                                </div>
                            </div>
                        ))}
                    </div>
                </div>

                <div class="column-block">
                    <h2 class="section-title">Education</h2>
                    <div class="timeline">
                        {education.map((item, i) => (
                            <div class="timeline-item">
                                <div class="timeline-marker"></div>
                                <div class="timeline-content">
                                    <span class="timeline-date">{item.date}</span>
                                    <h3 class="role-title">{item.role}</h3>
                                    <span class="company-name">{item.company}</span>
                                    <p class="description-text">{item.description}</p>
                                    {item.image && (
                                        <Image 
                                            src={item.image} 
                                            alt={item.company} 
                                            style={`
                                                margin-top: 0.5rem; 
                                                height: 13rem; 
                                                width: 100%; 
                                                object-fit: cover; 
                                                object-position: ${i === 0 ? '50% 90%' : 'center'}; 
                                                border-radius: 5px;
                                            `}
                                        />
                                    )}
                                </div>
                            </div>
                        ))}
                    </div>

                    <h2 class="section-title" style="margin-top: 3rem;">Languages & Soft Skills</h2>
                    <div class="timeline">
                        
                        <div class="timeline-item">
                            <div class="timeline-marker"></div>
                            <div class="timeline-content">
                                <h3 class="role-title">Languages</h3>
                                <p class="description-text" style="margin-top: 0.5rem;">
                                    <strong style="color: #fff;">Spanish:</strong> Native Speaker <br>
                                    <strong style="color: #fff;">English:</strong> Advanced (C1) - Technical Proficiency <br>
                                    <strong style="color: #fff;">French:</strong> Basic Understanding (A1/A2)
                                </p>
                            </div>
                        </div>

                        <div class="timeline-item">
                            <div class="timeline-marker"></div>
                            <div class="timeline-content">
                                <h3 class="role-title">Soft Skills</h3>
                                <p class="description-text" style="margin-top: 0.5rem;">
                                    <strong style="color: #fff;">Leadership & Mentoring:</strong> Experienced in guiding students through complex algorithmic concepts as an Academic Coach.
                                    <br><br>
                                    <strong style="color: #fff;">Teamwork:</strong> Proven ability to collaborate under pressure in timed competitions (ICPC, IEEEXtreme, ...).
                                    <br><br>
                                    <strong style="color: #fff;">Adaptability:</strong> Quick learner, able to pick up new programming languages and technologies in small time periods.
                                    <br><br>
                                    <strong style="color: #fff;">Problem-Solving:</strong> Strong analytical skills honed through competitive programming and academic projects.
                                    <br><br>
                                    <strong style="color: #fff;">Communication:</strong> Effective communicator, able to explain technical concepts clearly to diverse audiences.
                                </p>
                            </div>
                        </div>

                    </div>
                    
                    <figure>
                        <Image src={platicaImg} alt="Platica CPCFI" style="width: 100%; height: auto; margin-top: 1rem; border-radius: 5px; object-fit: cover; object-position: 50% 20%;" />
                        <figcaption style="text-align: center; font-size: 0.9rem; color: #aaa; margin-top: 0.5rem;">
                            Informative Lecture about CPCFI at UNAM School of Engineering.
                        </figcaption>
                    </figure>

                </div>
            </div>
            <br>
            <hr style="color: #4e4e4eb8">
            <section class="coursework-section" style="margin-bottom: 3rem; margin-top: 0rem;">
            <h2 class="section-title">Relevant Coursework</h2>
            <ul class="coursework-grid">
                {relevantCoursework.map((course) => (
                    <li class="course-item">{course}</li>
                ))}
            </ul>
        </section>
        </section>

    </div>

    <div id="portfolio" class="view-section hidden">
        <section class="projects-section">
            <h2 class="section-title">Outstanding Projects</h2>
            <div class="grid-projects">
                {projects.map((project) => (
                    <Card class="project-card-styled">
                        <div class="project-status-badge" style={`border-color: ${project.color}4D; background: ${project.color}1A;`}>
                            <span class="blinking-dot-project" style={`background-color: ${project.color}; box-shadow: 0 0 8px ${project.color}99;`}></span>
                            <span class="status-text" style={`color: ${project.color};`}>{project.status}</span>
                        </div>
                        <h3 class="project-title-styled">{project.title}</h3>
                        <p class="project-desc-styled">{project.description}</p>
                        <div class="tags-centered">
                            {project.tags.map((tag) => (
                                <span class="tag-pill" style={`color: ${project.color}; border-color: ${project.color}33;`}>{tag}</span>
                            ))}
                        </div>
                    </Card>
                ))}
            </div>
        </section>
    </div>

    <div id="contact" class="view-section hidden">
        <section class="contact-section">
            <div class="contact-container-grid">
                
                <div class="contact-info">
                    <h2 class="section-title">Let's Connect</h2>
                    <p class="contact-desc">
                        I'm always interested in new opportunities, interesting projects, and creative collaborations. 
                        Feel free to reach out!
                    </p>
                    
                    <div class="contact-methods">
                        <div class="method-item">
                            <img src="https://cdn.simpleicons.org/gmail/white" alt="Gmail" style="width: 1.5rem; height: 1.5rem; margin-right: 0.5rem;" />
                            <span>jafet.hernandez.ed@gmail.com</span>
                        </div>
                    </div>

                    <div class="social-links-contact">
                        {socialLinks.map(social => (
                            <a href={social.link} target="_blank" class="social-btn">
                                {social.image ? (
                                    <img src={social.image} alt={social.name} class="social-img"/>
                                ) : (
                                    <i class={social.icon}></i>
                                )}
                            </a>
                        ))}
                    </div>

                    <div class="download-container" style="margin-top: 2rem;">
                        <a href={import.meta.env.BASE_URL + "resume.pdf"} download="Jafet_Hernandez_Resume.pdf" class="download-btn">
                            Download Resume <i class="fas fa-download"></i>
                        </a>
                    </div>
                </div>

                <div class="contact-form-wrapper">
                    <h3 class="form-title">Send a Message</h3>
                    <form action="https://formspree.io/f/myzragqd" method="POST" class="contact-form">
                        <div class="form-group">
                            <label for="name">Name *</label>
                            <input type="text" id="name" name="name" placeholder="Your full name" required>
                        </div>
                        <div class="form-group">
                            <label for="email">Email *</label>
                            <input type="email" id="email" name="email" placeholder="your.email@example.com" required>
                        </div>
                        <div class="form-group">
                            <label for="subject">Subject *</label>
                            <input type="text" id="subject" name="subject" placeholder="What's this about?" required>
                        </div>
                        <div class="form-group">
                            <label for="message">Message *</label>
                            <textarea id="message" name="message" rows="4" placeholder="Tell me about your project or just say hello!" required></textarea>
                        </div>
                        <button type="submit" class="submit-btn">
                            Send Message ↗
                        </button>
                    </form>
                </div>

            </div>
        </section>
    </div>

  </main>
</Layout>

<script>
    const navItems = document.querySelectorAll('.nav-item');
    const sections = document.querySelectorAll('.view-section');

    navItems.forEach(item => {
        item.addEventListener('click', () => {
            navItems.forEach(nav => nav.classList.remove('active'));
            sections.forEach(sec => {
                sec.classList.remove('active');
                sec.classList.add('hidden');
            });
            item.classList.add('active');
            const targetId = item.getAttribute('data-target');
            const targetSection = document.getElementById(targetId);
            if (targetSection) {
                targetSection.classList.remove('hidden');
                targetSection.classList.add('active');
            }
        });
    });
</script>

<style>
  /* --- NAVBAR FLOTANTE --- */
  .top-nav {
      position: fixed;
      top: 20px;
      left: 0;
      right: 0;
      width: 100%;
      display: flex;
      justify-content: center;
      z-index: 1000;
      pointer-events: none;
  }

  .nav-container {
      display: flex;
      align-items: center;
      gap: 1.5rem;
      background: rgba(10, 10, 10, 0.7);
      backdrop-filter: blur(10px);
      padding: 0.5rem 2rem;
      border-radius: 50px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
      pointer-events: auto;
  }

  .nav-item {
      background: none;
      border: none;
      color: #aaa;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.8rem;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
      padding: 0.2rem 0;
  }

  .nav-item:hover { color: #fff; }
  .nav-item.active { color: #fff; font-weight: bold; }
  .nav-item.active::after {
      content: '';
      position: absolute; bottom: -4px; left: 50%; transform: translateX(-50%); width: 4px; height: 4px;
      background: #dd773c; border-radius: 50%;
  }

  /* Separador vertical */
  .nav-separator {
      width: 1px;
      height: 20px;
      background-color: rgba(255, 255, 255, 0.2);
      margin: 0 0.5rem;
  }

  /* BOTÓN DOWNLOAD RESUME NAVBAR */
  .nav-download-btn {
      background: rgba(221, 119, 60, 0.2); /* Color naranja */
      border: 1px solid #d4942d;
      backdrop-filter: blur(5px);
      color: #d8a57d;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.85rem;
      font-weight: bold;
      text-decoration: none;
      padding: 0.3rem 0.5rem;
      padding-left:1.0rem;
      border-radius: 50px;
      transition: all 0.2s ease;
      display: flex;
      text-align: center;
      gap: 0.5rem;
  }
  
  .nav-download-btn:hover {
      background: rgba(221, 119, 60, 0.7);
      color: #ece0d9;
      transform: translateY(-1px);
      box-shadow: 0 4px 10px rgba(221, 119, 60, 0.4);
  }

  /* --- LOGICA DE VISTAS --- */
  .view-section { opacity: 0; animation: fadeIn 0.5s forwards; width: 100%; }
  .view-section.hidden { display: none; }
  .view-section.active { display: block; }
  @keyframes fadeIn { to { opacity: 1; } }

  /* --- GENERAL LAYOUT --- */
  .container {
    max-width: 1100px;
    margin: auto;
    padding: 8rem 1rem 4rem 1rem;
    display: flex;
    flex-direction: column;
    min-height: 80vh;
  }

  .text-gradient {
    background: linear-gradient(45deg, #dd773c, #5d47a1);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  
  .section-title {
    font-size: 1.8rem;
    margin-bottom: 2rem;
    color: #fff;
    border-left: 5px solid #B79BFF;
    padding-left: 1rem;
    letter-spacing: -0.5px;
  }

  /* --- HERO GRID --- */
  .hero-grid {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 4rem;
      min-height: 60vh;
  }

  .image-column { flex-shrink: 0; }

  /* ESTILOS DE TU FOTO RESTAURADOS EXACTAMENTE */
  .profile-pic {
      width: 280px; 
      height: auto;
      border-radius: 5px; 
      border: 2px solid rgba(255, 255, 255, 0.1); 
      box-shadow: 0 0 25px rgba(221, 119, 60, 0.15); 
      transition: transform 0.3s ease;
      transform: rotate(2deg);
      filter: drop-shadow(0 0 10px rgba(114, 60, 221, 0.2));
  }
  
  .profile-pic:hover {
      transform: scale(1.05) rotate(-0.5deg);
  }

  .content-column { flex: 1; max-width: 650px; }

  .profile-header { text-align: center; padding: 1rem 0; }
  .profile-header h1 { font-size: 2.5rem; margin-bottom: 0.5rem; }
  .tagline { font-size: 1.1rem; color: #d7d045; font-family: 'JetBrains Mono', monospace; margin-bottom: 1.5rem; }
  .bio-text { max-width: 100%; margin: 0 auto 1.5rem auto; line-height: 1.6; opacity: 0.9; font-size: 1.2rem; color: #ccc; }
  .social-links { display: flex; gap: 1rem; justify-content: center; margin-bottom: 1.5rem; }
  
  @media (max-width: 900px) {
      .hero-grid { flex-direction: column; gap: 2rem; text-align: center; }
      .profile-pic { width: 200px; }
      .nav-container { flex-wrap: wrap; justify-content: center; border-radius: 20px; }
  }

  /* --- BOTONES SOCIALES --- */
  .social-btn {
    display: flex; align-items: center; justify-content: center;
    width: 3rem; height: 3rem; border-radius: 50%;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #e0e0e0; text-decoration: none;
    font-size: 1.5rem; transition: all 0.3s ease;
  }
  .social-btn:hover { background: rgb(173, 132, 37); color: #000; transform: translateY(-3px); border-color: #d4942d;}

  .cp-badges { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
  .cp-link {
    display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem 1rem;
    background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.1);
    border-radius: 8px; color: #fff; text-decoration: none; font-size: 0.9rem; transition: 0.3s;
  }
  .cp-link:hover { border-color: #d4942d; background: rgba(212, 162, 45, 0.1); }

  /* --- CORE EXPERTISE --- */
  .core-skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
      gap: 2rem;
  }
  .core-skill-item { display: flex; flex-direction: column; gap: 0.5rem; }
  .skill-info { display: flex; justify-content: space-between; font-family: 'JetBrains Mono', monospace; font-size: 0.9rem; }
  .skill-percentage { color: #2dd4bf; }
  .progress-container-core { width: 100%; height: 6px; background: rgba(255,255,255,0.1); border-radius: 3px; overflow: hidden; }
  .progress-bar-core { height: 100%; background: linear-gradient(90deg, #d4832d, #734091); border-radius: 3px; }

  /* --- SKILLS CARDS --- */
  .grid-skills { display: flex; flex-wrap: wrap; justify-content: center; gap: 1rem; }
  .skill-card-compact { width: fit-content !important; }
  .skill-content-compact { display: flex; align-items: center; justify-content: center; gap: 0.8rem; }
  .icon-small { font-size:1.1rem; flex-shrink: 0; text-align: center; }
  .skill-name-compact { font-size: 0.85rem; font-weight: 500; white-space: nowrap; }

  /* --- EXPERIENCE & EDUCATION --- */
  .columns-container {
      display: flex; flex-direction: row; justify-content: space-between;
      align-items: flex-start; gap: 3rem; width: 100%;
  }
  .column-block { flex: 1; min-width: 0; }

  @media (max-width: 768px) {
      .columns-container { flex-direction: column; gap: 4rem; }
  }

  .timeline { position: relative; border-left: 2px solid rgba(255, 255, 255, 0.1); padding-left: 2rem; }
  .timeline-item { position: relative; margin-bottom: 3rem; word-break: break-word; }
  .timeline-item:last-child { margin-bottom: 0; }
  .timeline-marker {
    position: absolute; left: -2.4rem; top: 0.3rem; 
    width: 12px; height: 12px; border-radius: 50%; background: #9575bf; box-shadow: 0 0 0 4px #0a0a0a;
  }
  .timeline-content { display: flex; flex-direction: column; gap: 0.3rem; }
  .timeline-date { font-family: 'JetBrains Mono', monospace; color: #6c757d; font-size: 0.85rem; margin-bottom: 0.2rem; }
  .role-title { font-size: 1.1rem; color: #fff; margin: 0; font-weight: bold; }
  .company-name { color: #B79BFF; font-size: 0.9rem; font-weight: 500; }
  .description-text { color: #b0b0b0; font-size: 0.9rem; line-height: 1.5; margin-top: 0.5rem; }

  /* --- PROJECTS --- */
  .grid-projects { display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 2.5rem; }
  
  .project-card-styled { 
      display: flex; flex-direction: column; align-items: center; gap: 1.5rem; padding: 2rem 1.5rem !important; 
  }
  
  .project-status-badge { display: inline-flex; align-items: center; gap: 0.6rem; padding: 0.4rem 0.8rem; border-radius: 20px; border: 1px solid; margin-bottom: 0.5rem; }
  .status-text { font-family: 'JetBrains Mono', monospace; font-size: 0.75rem; font-weight: bold; letter-spacing: 1px; text-transform: uppercase; }
  .blinking-dot-project { width: 8px; height: 8px; border-radius: 50%; animation: pulse-project 2s infinite ease-in-out; }
  @keyframes pulse-project { 0% { transform: scale(1); opacity: 1; } 50% { transform: scale(0.6); opacity: 0.5; } 100% { transform: scale(1); opacity: 1; } }
  
  .cursor { display: inline-block; width: 0.7ch; margin-left: 0.25rem; font-weight: 700; color: #d7d045; animation: blink-cursor 1s steps(1, start) infinite; -webkit-font-smoothing: antialiased; }
  @keyframes blink-cursor { 50% { opacity: 0; } }

  .project-title-styled { margin: 0; font-size: 1.4rem; font-weight: 700; text-align: center; }
  .project-desc-styled { font-size: 0.95rem; opacity: 0.8; line-height: 1.6; margin: 0; width: 100%; text-align: center; }
  
  .tags-centered { display: flex; flex-wrap: wrap; justify-content: center; gap: 0.6rem; margin-top: 0.5rem; }
  .tag-pill { font-family: 'JetBrains Mono', monospace; font-size: 0.7rem; background: rgba(255, 255, 255, 0.03); border: 1px solid; padding: 0.3rem 0.8rem; border-radius: 50px; }
  .social-img { width: 1.5rem; height: 1.5rem; object-fit: contain; transition: transform 0.3s ease; }
  .cp-img { width: 1.2rem; height: 1.2rem; object-fit: contain; filter: brightness(0) invert(1); }
  .cp-img.cses-logo { width: 2.5rem; height: auto; filter: brightness(0) invert(1); }

  /* --- CONTACT SECTION --- */
  .contact-container-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: start; }
  @media (max-width: 768px) { .contact-container-grid { grid-template-columns: 1fr; gap: 3rem; } }

  .contact-desc { color: #d1d5db; line-height: 1.6; margin-bottom: 2rem; }
  .contact-methods { display: flex; flex-direction: column; gap: 1rem; margin-bottom: 2rem; }
  .method-item { display: flex; align-items: center; gap: 1rem; color: #fff; font-family: 'JetBrains Mono', monospace; }
  .social-links-contact { display: flex; gap: 1rem; margin-bottom: 2rem; }

  /* Botón Descarga Contacto */
  .download-btn {
      display: inline-block;
      padding: 0.8rem 1.5rem;
      background: #dd773c;
      color: #000;
      font-weight: bold;
      text-decoration: none;
      border-radius: 8px;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .download-btn:hover { transform: translateY(-2px); box-shadow: 0 4px 12px rgba(183, 155, 255, 0.4); }

  /* Formulario */
  .contact-form-wrapper {
      background: rgba(15, 20, 25, 0.6); padding: 2rem; border-radius: 12px; border: 1px solid rgba(255, 255, 255, 0.05);
  }
  .form-title { margin-top: 0; margin-bottom: 1.5rem; font-size: 1.5rem; }
  .contact-form { display: flex; flex-direction: column; gap: 1.2rem; }
  .form-group { display: flex; flex-direction: column; gap: 0.5rem; }
  .form-group label { font-size: 0.85rem; font-weight: bold; color: #ccc; font-family: 'JetBrains Mono', monospace; }
  .form-group input, .form-group textarea {
      background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 6px; padding: 0.8rem;
      color: #fff; font-family: inherit; transition: border-color 0.3s;
  }
  .form-group input:focus, .form-group textarea:focus { outline: none; border-color: #B79BFF; }
  .submit-btn {
      margin-top: 1rem; padding: 0.8rem; background: #e0e0e0; color: #000; border: none; border-radius: 6px;
      font-weight: bold; cursor: pointer; transition: background 0.3s;
  }
  .submit-btn:hover { background: #fff; }

  /* --- RELEVANT COURSEWORK --- */
  .coursework-grid {
      display: grid;
      /* Crea columnas automáticas que se ajustan al espacio (mínimo 200px ancho) */
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
      gap: 1rem;
      list-style: none;
      padding: 0;
      margin: 0;
  }

  .course-item {
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 8px; /* Bordes redondeados sutiles */
      padding: 0.5rem 1.2rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.8rem;
      color: #d1d5db;
      text-align: center;
      transition: all 0.3s ease;
      cursor: default;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 2.5rem;
  }

  .course-item:hover {
      border-color: #dd773c; /* Tu color de acento naranja */
      background: rgba(221, 119, 60, 0.1);
      color: #fff;
      transform: translateY(-2px);
  }

  .fi-pic {
      width: 300px; 
      height: auto;
      border-radius: 5px; 
      border: 2px solid rgba(237, 229, 229, 0.1); 
      box-shadow: 0 0 10px rgba(221, 119, 60, 0.15); 
      transition: transform 0.3s ease;
      filter: drop-shadow(0 0 3px rgba(114, 60, 221, 0.2));
      margin: auto;
      display: block;
      margin-left: -2.5rem;
  }
  
  .fi-pic:hover {
      transform: scale(1.005);
  }
</style>